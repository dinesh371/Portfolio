<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>T. Dinesh Portfolio</title>
  <!-- Google Fonts: Orbitron for futuristic look -->
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #101018;
      --card-bg: #181828;
      --text: #f1f6fb;
      --accent: #00ffe7;
      --accent-glow: 0 0 16px #00ffe7, 0 0 32px #00ffe7;
      --shadow: 0 2px 16px rgba(0,255,231,0.08);
    }
    html, body {
      height: 100%;
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-family: 'Montserrat', Arial, sans-serif;
      letter-spacing: 0.01em;
      transition: background 0.3s, color 0.3s;
      scroll-behavior: smooth;
      overflow-x: hidden;
    }
    body {
      min-height: 100vh;
    }
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 32px 48px 24px 48px;
      background: transparent;
      position: sticky;
      top: 0;
      z-index: 10;
      backdrop-filter: blur(3px);
    }
    h1 {
      font-family: 'Orbitron', sans-serif;
      font-size: 2.5rem;
      color: var(--accent);
      text-shadow: var(--accent-glow);
      margin: 0;
      letter-spacing: 0.04em;
    }
    nav a {
      color: var(--text);
      text-decoration: none;
      margin-left: 32px;
      font-size: 1.1rem;
      font-weight: 700;
      position: relative;
      letter-spacing: 0.03em;
      transition: color 0.2s;
    }
    nav a:hover {
      color: var(--accent);
      text-shadow: var(--accent-glow);
    }
    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 80vh;
      position: relative;
      z-index: 1;
      margin-bottom: 48px;
    }
    .hero-canvas {
      width: 360px;
      height: 360px;
      margin-bottom: 32px;
      border-radius: 24px;
      box-shadow: var(--shadow);
      background: #0a0a13;
    }
    .hero h2 {
      font-family: 'Orbitron', sans-serif;
      font-size: 2.2rem;
      color: var(--accent);
      text-shadow: var(--accent-glow);
      margin-bottom: 18px;
      letter-spacing: 0.03em;
    }
    .hero p {
      font-size: 1.2rem;
      max-width: 600px;
      text-align: center;
      margin-bottom: 16px;
      color: #b5eaff;
    }
    section {
      max-width: 950px;
      margin: 0 auto 60px auto;
      padding: 0 24px;
      background: transparent;
    }
    h3 {
      text-align: center;
      font-size: 2rem;
      color: var(--accent);
      text-shadow: var(--accent-glow);
      margin-bottom: 32px;
      font-family: 'Orbitron', sans-serif;
      letter-spacing: 0.03em;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 32px;
      margin-bottom: 24px;
    }
    .card {
      background: var(--card-bg);
      border-radius: 18px;
      padding: 32px 24px;
      box-shadow: var(--shadow);
      transition: transform 0.2s, box-shadow 0.2s;
      position: relative;
      overflow: hidden;
    }
    .card:hover {
      transform: translateY(-6px) scale(1.025);
      box-shadow: 0 4px 32px #00ffe788;
    }
    .card h4 {
      color: var(--accent);
      margin-bottom: 12px;
      font-family: 'Orbitron', sans-serif;
      font-size: 1.2rem;
      letter-spacing: 0.02em;
    }
    .card p {
      color: #c3f5ff;
      font-size: 1rem;
      margin: 0;
    }
    .contact {
      text-align: center;
    }
    .contact p, .contact a {
      color: #b5eaff;
      font-size: 1.1rem;
      margin: 7px 0;
      text-decoration: none;
    }
    footer {
      background: transparent;
      padding: 32px 0 24px 0;
      text-align: center;
      color: #6ee7ff;
      font-size: 1rem;
      letter-spacing: 0.04em;
      font-family: 'Orbitron', sans-serif;
    }
    /* Form styling */
    form {
      display: flex;
      flex-direction: column;
      gap: 16px;
      max-width: 420px;
      margin: 0 auto 18px auto;
    }
    input, textarea {
      padding: 14px;
      border-radius: 8px;
      border: 1px solid #00ffe7;
      background: #101018;
      color: var(--text);
      font-size: 1rem;
      font-family: 'Montserrat', sans-serif;
      outline: none;
      transition: border 0.2s;
    }
    input:focus, textarea:focus {
      border: 1.5px solid #00ffe7;
      box-shadow: var(--accent-glow);
    }
    button.submit-btn {
      background: linear-gradient(90deg, #00ffe7 40%, #1e90ff 100%);
      border: none;
      color: #101018;
      padding: 14px;
      font-size: 1.1rem;
      font-family: 'Orbitron', sans-serif;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      letter-spacing: 0.04em;
      box-shadow: 0 2px 16px #00ffe799;
      transition: background 0.2s, color 0.2s;
    }
    button.submit-btn:hover {
      background: linear-gradient(90deg, #1e90ff 40%, #00ffe7 100%);
      color: #fff;
    }
    /* Custom neon cursor */
    .custom-cursor {
      position: fixed;
      top: 0; left: 0;
      width: 28px; height: 28px;
      border: 2.5px solid var(--accent);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      box-shadow: 0 0 16px #00ffe7, 0 0 32px #00ffe7;
      mix-blend-mode: lighten;
      transition: transform 0.1s;
      will-change: transform;
    }
    @media (max-width: 600px) {
      header { flex-direction: column; padding: 18px 8px; }
      nav a { margin-left: 18px; font-size: 1rem; }
      .hero-canvas { width: 98vw; height: 220px; }
      .hero h2 { font-size: 1.3rem; }
      .grid { grid-template-columns: 1fr; gap: 18px; }
      .card { padding: 18px 10px; }
    }
  </style>
</head>
<body>
  <div class="custom-cursor"></div>
  <header>
    <h1>T. Dinesh</h1>
    <nav>
      <a href="#">Home</a>
      <a href="#services">Services</a>
      <a href="#experience">Experience</a>
      <a href="#projects">Projects</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <section class="hero" id="hero">
    <canvas class="hero-canvas" id="three-canvas"></canvas>
    <h2>Empowering Businesses with Scalable Cloud & IT Solutions</h2>
    <p>
      Hi, I'm <strong>T. Dinesh</strong> — an Azure-certified IT Systems Administrator helping organizations modernize their infrastructure, secure their data, and build reliable digital experiences.
    </p>
  </section>

  <section id="services">
    <h3>Core Services</h3>
    <div class="grid">
      <div class="card"><h4>Azure Cloud Services</h4><p>Cloud migration, setup, automation, and cost optimization using Azure infrastructure.</p></div>
      <div class="card"><h4>Web Development</h4><p>Creation of responsive, SEO-optimized websites and apps with hosting and CI/CD deployment.</p></div>
      <div class="card"><h4>System Administration</h4><p>Windows Server, Linux, VMware & Hyper-V management for high-availability setups.</p></div>
      <div class="card"><h4>Security & Monitoring</h4><p>Threat prevention and monitoring with Sophos, CrowdStrike, Arctic Wolf & SIEM tools.</p></div>
      <div class="card"><h4>IT Support & Maintenance</h4><p>Reliable troubleshooting, upgrades, patching, and preventive maintenance support.</p></div>
      <div class="card"><h4>IT Consulting & Infrastructure Planning</h4><p>Infrastructure design, roadmap building, cost analysis, and risk mitigation advisory.</p></div>
    </div>
  </section>

  <section id="experience">
    <h3>Experience & Certifications</h3>
    <div class="grid">
      <div class="card"><h4>8+ Years Experience</h4><p>Proven experience in cloud migration, IT operations, healthcare and government tech deployments.</p></div>
      <div class="card"><h4>AZ-104 Azure Administrator</h4><p>Certified Microsoft Azure professional with production-level deployments and automation.</p></div>
      <div class="card"><h4>MTA & MCP</h4><p>Microsoft Technology Associate and Certified Professional with deep system knowledge.</p></div>
      <div class="card"><h4>Office 365 Admin</h4><p>Tenant setup, user access, policies, integrations, and security best practices across Microsoft 365.</p></div>
    </div>
  </section>

  <section id="projects">
    <h3>Key Projects</h3>
    <div class="grid">
      <div class="card"><h4>Mobile Telemedicine</h4><p>Remote patient monitoring and diagnostics with mobile-first health platform.</p></div>
      <div class="card"><h4>NITI Aayog District Monitoring</h4><p>Government platform for real-time monitoring and analytics of district-level development.</p></div>
      <div class="card"><h4>Community Mobilizer App</h4><p>Grassroots digital solution for social empowerment and community engagement.</p></div>
      <div class="card"><h4>Door-to-Door Logistics Platform</h4><p>Real-time logistics tracking with routing, dispatch, and notification modules.</p></div>
    </div>
  </section>

  <section id="contact" class="contact">
    <h3>Contact Me</h3>
    <form>
      <input type="text" placeholder="Your Name" required />
      <input type="email" placeholder="Your Email" required />
      <textarea placeholder="Your Message" rows="5" required></textarea>
      <button class="submit-btn" type="submit">Send Message</button>
    </form>
    <p><strong>Email:</strong> <a href="mailto:dineshtankala85@outlook.com">dineshtankala85@outlook.com</a></p>
    <p><strong>Phone:</strong> +91-8331956172 (9 AM – 6 PM IST)</p>
    <p><strong>LinkedIn:</strong> <a href="https://linkedin.com/in/dinesh-tankala" target="_blank">LinkedIn Profile</a></p>
    <p><strong>Location:</strong> Visakhapatnam, India | Open to remote projects</p>
  </section>

  <footer>
    &copy; 2024 T. Dinesh. All rights reserved.
  </footer>

  <!-- Three.js CDN -->
  <script src="https://cdn.jsdelivr.net/npm/three@0.154.0/build/three.min.js"></script>
  <script>
    // Neon custom cursor
    const cursor = document.querySelector('.custom-cursor');
    document.addEventListener('mousemove', e => {
      cursor.style.transform = `translate(${e.clientX - 14}px, ${e.clientY - 14}px)`;
    });

    // Simple Three.js spinning cube in hero
    const canvas = document.getElementById('three-canvas');
    const renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true });
    renderer.setSize(canvas.offsetWidth, canvas.offsetHeight);
    renderer.setClearColor(0x101018, 0);

    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(50, canvas.offsetWidth / canvas.offsetHeight, 0.1, 1000);
    camera.position.z = 4;

    const geometry = new THREE.BoxGeometry(1.2, 1.2, 1.2);
    const material = new THREE.MeshStandardMaterial({
      color: 0x00ffe7,
      emissive: 0x00ffe7,
      emissiveIntensity: 0.6,
      metalness: 0.4,
      roughness: 0.3
    });
    const cube = new THREE.Mesh(geometry, material);
    scene.add(cube);

    const light = new THREE.PointLight(0x00ffe7, 2, 100);
    light.position.set(4, 4, 6);
    scene.add(light);

    function animate() {
      cube.rotation.x += 0.012;
      cube.rotation.y += 0.014;
      renderer.render(scene, camera);
      requestAnimationFrame(animate);
    }
    animate();

    // Responsive canvas
    window.addEventListener('resize', () => {
      const w = canvas.offsetWidth, h = canvas.offsetHeight;
      renderer.setSize(w, h);
      camera.aspect = w / h;
      camera.updateProjectionMatrix();
    });

    // Smooth fade-in for sections
    document.querySelectorAll('section').forEach((sec, i) => {
      sec.style.opacity = 0;
      sec.style.transform = "translateY(40px)";
      setTimeout(() => {
        sec.style.transition = "opacity 1.1s cubic-bezier(.77,0,.18,1), transform 1.1s cubic-bezier(.77,0,.18,1)";
        sec.style.opacity = 1;
        sec.style.transform = "translateY(0)";
      }, 350 + i * 180);
    });
  </script>
</body>
</html>
